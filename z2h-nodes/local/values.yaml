syncer:
  extraArgs:
  - --tls-san=z2h.z2hpro.online
  # - --tls-san=20.55.10.216 Azure

# sync:
#   persistentvolumes:
#     enabled: true
#   # If you want to create custom storage classes
#   # inside the vcluster.
#   storageclasses:
#     enabled: true
